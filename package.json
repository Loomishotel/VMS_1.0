{
  "name": "visitor-management-system",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=!api-server",
    "build": "turbo run build --filter=!api-server"
  },
  "devDependencies": {
    "turbo": "^2.0.4"
  }
}

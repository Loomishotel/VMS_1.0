{
  "buildCommand": "cd ../.. && npm install && npx turbo run build --filter=visitor-registration",
  "outputDirectory": "dist",
  "installCommand": "echo 'Install handled by buildCommand'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
